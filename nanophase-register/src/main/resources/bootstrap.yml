spring:
  cloud:
    nacos:
      discovery:
        # Nacos server的地址
        server-addr: 127.0.0.1:8848
#      config:
#        server-addr: 127.0.0.1:8848
  application:
    # Nacos server的应用名
    name: nanophase-config
server:
  port: 10086