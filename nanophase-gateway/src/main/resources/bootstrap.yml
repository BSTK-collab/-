spring:
  application:
    name: nanophase-gateway
  cloud:
    nacos:
      discovery:
        # Nacos地址
        server-addr: 127.0.0.1:8848
      config:
        server-addr: http://localhost:8848
        file-extension: yml