package com.nanophase.algorithm.thread;

/**
 * @author zhj
 * @date 2021-04-06
 * @apiNote yield；native方法，作用是当前线程做出让步，停止使用CPU，让CPU重新选择线程
 * 不排除CPU再次选择当前线程。一般在防止CPU一直被霸占时使用
 */
public class TestYield {

    public static void main(String[] args) {
    }
}
